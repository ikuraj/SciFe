package insynth
package streams
package dependent

import org.scalatest._
import org.scalatest.matchers._

import streams.{ light => e }

import util._
import util.format._
import util.logging._
import common._

class DependentBenchmarks extends FunSuite with Matchers with
	HasLogger with ProfileLogger {  
  import Checks._
  import Structures._
  
}
